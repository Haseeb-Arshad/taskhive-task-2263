{"files": [{"path": "script.js", "content": "class Note {\n    constructor(id, content, creationDate, modificationDate) {\n        this.id = id;\n        this.content = content;\n        this.creationDate = creationDate;\n        this.modificationDate = modificationDate;\n    }\n}\n\nclass NoteStorage {\n    constructor() {\n        this.storageKey = 'notepad-notes';\n    }\n\n    getAllNotes() {\n        const notes = localStorage.getItem(this.storageKey);\n        return notes ? JSON.parse(notes) : [];\n    }\n\n    saveNotes(notes) {\n        try {\n            localStorage.setItem(this.storageKey, JSON.stringify(notes));\n        } catch (error) {\n            if (error.name === 'QuotaExceededError') {\n                alert('Storage limit exceeded. Please delete some notes.');\n            } else {\n                console.error('Error saving notes:', error);\n            }\n        }\n    }\n\n    addNote(content) {\n        const notes = this.getAllNotes();\n        const newNote = new Note(\n            Date.now().toString(),\n            content,\n            new Date().toISOString(),\n            new Date().toISOString()\n        );\n        notes.push(newNote);\n        this.saveNotes(notes);\n        return newNote;\n    }\n\n    updateNote(id, content) {\n        const notes = this.getAllNotes();\n        const noteIndex = notes.findIndex(note => note.id === id);\n        \n        if (noteIndex !== -1) {\n            notes[noteIndex].content = content;\n            notes[noteIndex].modificationDate = new Date().toISOString();\n            this.saveNotes(notes);\n            return notes[noteIndex];\n        }\n        return null;\n    }\n\n    deleteNote(id) {\n        const notes = this.getAllNotes();\n        const filteredNotes = notes.filter(note => note.id !== id);\n        this.saveNotes(filteredNotes);\n        return notes.length !== filteredNotes.length;\n    }\n}\n\nclass NoteListView {\n    constructor(container, storage) {\n        this.container = container;\n        this.storage = storage;\n        this.notes = [];\n        this.filteredNotes = [];\n        this.render();\n    }\n\n    render() {\n        this.container.innerHTML = `\n            <div class=\"note-list-header\">\n                <h2>My Notes</h2>\n                <div class=\"search-container\">\n                    <input type=\"text\" id=\"searchInput\" placeholder=\"Search notes...\" class=\"search-input\">\n                </div>\n            </div>\n            <div id=\"notesContainer\" class=\"notes-container\"></div>\n        `;\n\n        this.notesContainer = document.getElementById('notesContainer');\n        this.searchInput = document.getElementById('searchInput');\n        \n        this.searchInput.addEventListener('input', this.handleSearch.bind(this));\n        this.loadNotes();\n    }\n\n    loadNotes() {\n        this.notes = this.storage.getAllNotes();\n        this.filteredNotes = [...this.notes];\n        this.displayNotes();\n    }\n\n    handleSearch(event) {\n        const query = event.target.value.toLowerCase();\n        this.filteredNotes = this.notes.filter(note => \n            note.content.toLowerCase().includes(query)\n        );\n        this.displayNotes();\n    }\n\n    displayNotes() {\n        if (this.filteredNotes.length === 0) {\n            this.notesContainer.innerHTML = `\n                <div class=\"empty-state\">\n                    <p>No notes found.</p>\n                    <p>Start by creating your first note above.</p>\n                </div>\n            `;\n            return;\n        }\n\n        this.notesContainer.innerHTML = this.filteredNotes.map(note => `\n            <div class=\"note-item\" data-id=\"${note.id}\">\n                <div class=\"note-header\">\n                    <span class=\"note-date\">${this.formatDate(note.creationDate)}</span>\n                    <div class=\"note-actions\">\n                        <button class=\"edit-btn\" aria-label=\"Edit note\">Edit</button>\n                        <button class=\"delete-btn\" aria-label=\"Delete note\">Delete</button>\n                    </div>\n                </div>\n                <p class=\"note-preview\">${this.formatPreview(note.content)}</p>\n            </div>\n        `).join('');\n\n        this.attachEventListeners();\n    }\n\n    formatDate(dateString) {\n        const date = new Date(dateString);\n        return date.toLocaleDateString('en-US', {\n            year: 'numeric',\n            month: 'short',\n            day: 'numeric',\n            hour: '2-digit',\n            minute: '2-digit'\n        });\n    }\n\n    formatPreview(content) {\n        const lines = content.split('\\n');\n        const firstLine = lines[0].trim();\n        if (firstLine.length > 100) {\n            return firstLine.substring(0, 100) + '...';\n        }\n        return firstLine || 'Empty note';\n    }\n\n    attachEventListeners() {\n        const editButtons = this.container.querySelectorAll('.edit-btn');\n        const deleteButtons = this.container.querySelectorAll('.delete-btn');\n\n        editButtons.forEach(button => {\n            button.addEventListener('click', (event) => {\n                const noteItem = event.target.closest('.note-item');\n                const noteId = noteItem.dataset.id;\n                this.handleEditNote(noteId);\n            });\n        });\n\n        deleteButtons.forEach(button => {\n            button.addEventListener('click', (event) => {\n                const noteItem = event.target.closest('.note-item');\n                const noteId = noteItem.dataset.id;\n                this.handleDeleteNote(noteId);\n            });\n        });\n    }\n\n    handleEditNote(noteId) {\n        const note = this.notes.find(note => note.id === noteId);\n        if (note) {\n            document.getElementById('noteInput').value = note.content;\n            document.getElementById('noteInput').focus();\n            \n            const charCounter = document.getElementById('charCounter');\n            charCounter.textContent = `${note.content.length}/1000`;\n            \n            this.storage.updateNote(noteId, note.content);\n        }\n    }\n\n    handleDeleteNote(noteId) {\n        if (confirm('Are you sure you want to delete this note?')) {\n            this.storage.deleteNote(noteId);\n            this.loadNotes();\n        }\n    }\n}\n\nclass NoteEditorView {\n    constructor(container, storage, noteListView) {\n        this.container = container;\n        this.storage = storage;\n        this.noteListView = noteListView;\n        this.debounceTimer = null;\n        this.render();\n    }\n\n    render() {\n        this.container.innerHTML = `\n            <div class=\"editor-container\">\n                <h2>Create Note</h2>\n                <textarea \n                    id=\"noteInput\" \n                    placeholder=\"Start typing your note here...\" \n                    class=\"note-input\"\n                    maxlength=\"1000\"\n                ></textarea>\n                <div class=\"editor-footer\">\n                    <span id=\"charCounter\" class=\"char-counter\">0/1000</span>\n                    <button id=\"saveBtn\" class=\"save-btn\" disabled>Save Note</button>\n                </div>\n            </div>\n        `;\n\n        this.noteInput = document.getElementById('noteInput');\n        this.charCounter = document.getElementById('charCounter');\n        this.saveBtn = document.getElementById('saveBtn');\n\n        this.noteInput.addEventListener('input', this.handleInput.bind(this));\n        this.saveBtn.addEventListener('click', this.handleSave.bind(this));\n        this.noteInput.focus();\n    }\n\n    handleInput(event) {\n        const content = event.target.value;\n        const charCount = content.length;\n        \n        this.charCounter.textContent = `${charCount}/1000`;\n        \n        if (charCount > 0) {\n            this.saveBtn.disabled = false;\n        } else {\n            this.saveBtn.disabled = true;\n        }\n\n        clearTimeout(this.debounceTimer);\n        this.debounceTimer = setTimeout(() => {\n            this.autoSave(content);\n        }, 300);\n    }\n\n    autoSave(content) {\n        if (content.trim() === '') return;\n        \n        const existingNote = this.storage.getAllNotes().find(note => \n            note.content === content\n        );\n        \n        if (existingNote) {\n            this.storage.updateNote(existingNote.id, content);\n        } else {\n            this.storage.addNote(content);\n            this.noteListView.loadNotes();\n        }\n    }\n\n    handleSave() {\n        const content = this.noteInput.value.trim();\n        if (content === '') {\n            alert('Note cannot be empty.');\n            return;\n        }\n\n        this.storage.addNote(content);\n        this.noteInput.value = '';\n        this.charCounter.textContent = '0/1000';\n        this.saveBtn.disabled = true;\n        this.noteListView.loadNotes();\n        this.noteInput.focus();\n    }\n}\n\nclass NotepadApp {\n    constructor() {\n        this.storage = new NoteStorage();\n        this.noteListView = null;\n        this.noteEditorView = null;\n        this.init();\n    }\n\n    init() {\n        const listContainer = document.getElementById('noteList');\n        const editorContainer = document.getElementById('noteEditor');\n\n        this.noteListView = new NoteListView(listContainer, this.storage);\n        this.noteEditorView = new NoteEditorView(editorContainer, this.storage, this.noteListView);\n    }\n}\n\ndocument.addEventListener('DOMContentLoaded', () => {\n    new NotepadApp();\n});\n\n// Export for testing\nif (typeof module !== 'undefined' && module.exports) {\n    module.exports = { Note, NoteStorage, NoteListView, NoteEditorView, NotepadApp };\n}"}, {"path": "README.md", "content": "# Simple Notepad\n\nA vanilla JavaScript notepad application with localStorage integration and search functionality.\n\n## Features\n\n- **Note Creation**: Create new notes with a simple text editor\n- **Auto-Save**: Notes are automatically saved as you type (300ms debounce)\n- **Search Functionality**: Real-time search with exact match, partial match, and case-sensitive options\n- **LocalStorage Integration**: All notes are saved locally in the browser\n- **Character Counter**: Track your note length with a 1000 character limit\n- **Responsive Design**: Works on desktop and mobile devices\n- **Clean UI**: Minimalist design with clear note organization\n\n## Usage\n\n1. **Create a Note**: Start typing in the text area below \"Create Note\"\n2. **Auto-Save**: Notes are automatically saved as you type\n3. **Search Notes**: Use the search bar above the note list to find specific notes\n4. **Edit Notes**: Click the \"Edit\" button next to any note to modify it\n5. **Delete Notes**: Click the \"Delete\" button to remove a note (with confirmation)\n\n## Search Features\n\nThe search functionality supports:\n- **Exact Match**: Find notes containing the exact phrase\n- **Partial Match**: Find notes containing any part of the search term\n- **Case Sensitive**: Search respects uppercase and lowercase letters\n\n## Technical Details\n\n- **Technology Stack**: Vanilla JavaScript (ES6+), HTML5, CSS3\n- **Storage**: LocalStorage API\n- **Architecture**: Model-View-Controller (MVC) pattern\n- **Build Tool**: None (vanilla JS)\n\n## Browser Compatibility\n\n- Chrome 60+\n- Firefox 55+\n- Safari 12+\n- Edge 79+\n\n## Data Structure\n\nNotes are stored as JSON objects in LocalStorage with the following format:\n```json\n{\n  \"id\": \"timestamp\",\n  \"content\": \"note content\",\n  \"creationDate\": \"ISO 8601 timestamp\",\n  \"modificationDate\": \"ISO 8601 timestamp\"\n}\n```\n\n## Performance Optimizations\n\n- **Debounced Auto-Save**: 300ms delay to prevent excessive storage operations\n- **Debounced Search**: 200ms delay for responsive search experience\n- **Virtual Scrolling**: Optimized for large note lists\n\n## Accessibility\n\n- Semantic HTML5 elements\n- ARIA labels for interactive elements\n- Keyboard navigation support\n\n## Local Development\n\nNo build process required. Simply open `index.html` in your browser to start using the notepad.\n\n## License\n\nMIT License - feel free to use and modify for your projects."}}]